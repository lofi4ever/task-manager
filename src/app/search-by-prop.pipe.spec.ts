import { SearchByPropPipe } from './search-by-prop.pipe';

describe('SearchByPropPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByPropPipe();
    expect(pipe).toBeTruthy();
  });
});
