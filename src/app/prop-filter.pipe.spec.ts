import { PropFilterPipe } from './prop-filter.pipe';

describe('PropFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PropFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
